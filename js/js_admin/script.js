 const activeblue = document.querySelectorAll('list-group-item123');    

 activeblue.addEventListener("click", () => {
    gallery.classList.add("active");
  }); 
  activeblue.addEventListener("click", () => {
    gallery.classList.remove("active");
  })